<div class="tasks-section py-5">
  <div class="container">
    <div class="card shadow-sm border-0 p-4">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-5 fw-bold">Задачи</h1>
        <%= link_to "Добавить задачу", new_task_path, class: "btn btn-primary btn-lg" %>
      </div>

      <table class="table table-hover align-middle">
        <thead class="table-dark">
          <tr>
            <th>№</th>
            <% model_class = Task %>
            <% model_attributes = model_class.column_names - ["id", "created_at", "updated_at"] %>

            <% model_attributes.each do |attr| %>
              <% if attr == "status" %>
                <th style="width: 120px;"><%= model_class.column_names_rus[attr] %></th>
              <% else %>
                <th><%= model_class.column_names_rus[attr] %></th>
              <% end %>
            <% end %>
            <th class="text-center" style="width: 150px;">Действия</th>
          </tr>
        </thead>
        <tbody>
          <% @tasks.each_with_index do |task, index| %>
            <tr>
              <td><%= (@current_page - 1) * 5 + index + 1 %></td>
              <% model_attributes.each do |attr| %>
                <td>
                  <% if attr == "due_date" %>
                    <%= task.formatted_due_date %>
                  <% elsif attr == "project_id" %>
                    <%= task.project_name %>
                  <% elsif attr == "assignee_id" %>
                    <%= task.assignee_name %>
                  <% elsif attr == "status" %>
                    <%= task.current_status %>
                  <% else %>
                    <%= task.send(attr) %>
                  <% end %>
                </td>
              <% end %>
              <td class="text-center">
                <%= form_with url: edit_task_path(task), method: :get, local: true, class: "d-inline" do %>
                  <%= button_to "✏️", edit_task_path(task), method: :get, class: "btn btn-outline-light btn-sm me-2" %>
                <% end %>
                <%= form_with url: task_path(task), method: :delete, local: true, class: "d-inline" do %>
                  <%= button_to "🗑", task_path(task), method: :delete, data: { confirm: "Вы уверены, что хотите удалить эту задачу?" }, class: "btn btn-outline-light btn-sm" %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <!-- Пагинация -->
    <div class="d-flex justify-content-center mt-4">
      <% if @current_page > 1 %>
        <%= link_to "Предыдущая", tasks_path(page: @current_page - 1), class: "btn btn-outline-secondary me-2" %>
      <% end %>

      <% (1..@total_pages).each do |page| %>
        <%= link_to page, tasks_path(page: page), class: "btn btn-primary me-2 #{'active' if page == @current_page}" %>
      <% end %>

      <% if @current_page < @total_pages %>
        <%= link_to "Следующая", tasks_path(page: @current_page + 1), class: "btn btn-outline-secondary" %>
      <% end %>
    </div>

    <!-- Кнопка возврата к разделам -->
    <div class="text-center mt-4">
      <%= link_to "🏠 Вернуться назад", root_path(anchor: "sections"), class: "btn btn-secondary btn-lg" %>
    </div>
  </div>
</div>

<!-- Стили -->
<style>
  /* Фон, как на других страницах */
  .tasks-section {
    background: linear-gradient(135deg, #e6f0ff, #f8f9fa);
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* Карточка */
  .card {
    background: #fff;
    border-radius: 12px;
    box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1);
  }

  /* Кнопка возврата */
  .btn-secondary {
    background-color: #6c757d;
    border-color: #6c757d;
  }

  .btn-secondary:hover {
    background-color: #5a6268;
    border-color: #545b62;
  }
</style>
