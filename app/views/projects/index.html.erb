<div class="projects-section py-5">
  <div class="container">
    <div class="card shadow-sm border-0 p-4">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-5 fw-bold">Проекты</h1>
        <%= link_to "Добавить проект", new_project_path, class: "btn btn-primary btn-lg" %>
      </div>

      <table class="table table-hover align-middle">
        <thead class="table-dark">
          <tr>
            <th>№</th>
            <% model_attributes = Project.column_names - ["id", "created_at", "updated_at"] %>
            <% model_attributes.each do |attr| %>
              <th><%= Project.column_names_rus[attr] %></th>
            <% end %>
            <th class="text-center">Действия</th>
          </tr>
        </thead>
        <tbody>
          <% @projects.each_with_index do |project, index| %>
            <tr>
              <td><%= index + 1 %></td>
              <% model_attributes.each do |attr| %>
                <td><%= project.send(attr) %></td>
              <% end %>
              <td class="text-center">
                <%= form_with url: edit_project_path(project), method: :get, local: true, class: "d-inline" do %>
                  <%= button_to "✏️", edit_project_path(project), method: :get, class: "btn btn-outline-light btn-sm me-2" %>
                <% end %>
                <%= form_with url: project_path(project), method: :delete, local: true, class: "d-inline" do %>
                  <%= button_to "🗑", project_path(project), method: :delete, data: { confirm: "Вы уверены, что хотите удалить этот проект?" }, class: "btn btn-outline-light btn-sm" %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <!-- Кнопка возврата к разделам -->
    <div class="text-center mt-4">
      <%= link_to "🏠 Вернуться назад", root_path(anchor: "sections"), class: "btn btn-secondary btn-lg" %>
    </div>
  </div>
</div>

<!-- Стили -->
<style>
  /* Фон */
  .projects-section {
    background: linear-gradient(135deg, #e6f0ff, #f8f9fa);
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* Тень */
  .card {
    background: #fff;
    border-radius: 12px;
    box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1);
  }

  /* Кнопка возврата */
  .btn-secondary {
    background-color: #6c757d;
    border-color: #6c757d;
  }

  .btn-secondary:hover {
    background-color: #5a6268;
    border-color: #545b62;
  }
</style>
